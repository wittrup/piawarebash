#!/usr/bin/env bash
# Connect with Kids

if [ -n "$1" ]; then # Argument no 1 is set
	lopo=$(sudo netstat -tlp | grep localhost | grep -v mysql | grep $1 | awk '{print $4}' | sed 's/[^0-9]*//g')
	ssh -p $lopo pi@localhost
else
	sudo netstat -tlp | grep localhost | grep -v mysql
fi
